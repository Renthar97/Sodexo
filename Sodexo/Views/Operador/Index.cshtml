@model IEnumerable<Sodexo.Entities.Entities.Operador>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Operador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Operador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OperadorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OperadorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OperadorId })
        </td>
    </tr>
}

</table>
