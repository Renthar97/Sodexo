@model IEnumerable<Sodexo.Entities.Entities.ReposicionBloqueo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bloqueo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostoReposicion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostoEnvio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bloqueo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostoReposicion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostoEnvio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReposicionBloqueoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReposicionBloqueoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReposicionBloqueoId })
        </td>
    </tr>
}

</table>
